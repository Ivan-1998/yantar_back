image: node:latest

stages:
  - deploy

cache:
  paths:
    - node_modules/

before_script:
  - cd ../../
  - git pull

script:
  - git pull origin master
  - pm2 delete "npm run serve"
  - pm2 start "npm run serve"

only: 
  - master