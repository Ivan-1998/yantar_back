image: node:latest

stages:
  - deploy

main-build:
  stage: deploy
  before_script:
    - cd ../../
    - git pull
  script:
    - git pull origin master
    - pm2 delete "npm run serve"
    - pm2 start "npm run serve"
  only: 
    - master